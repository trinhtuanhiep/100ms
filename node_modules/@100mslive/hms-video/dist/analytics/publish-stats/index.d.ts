import { EventBus } from '../../events/EventBus';
import { IStore } from '../../sdk/store';
export declare class PublishStatsAnalytics {
    private store;
    private eventBus;
    private readonly sampleWindowSize;
    private readonly pushInterval;
    private shouldSendEvent;
    private sequenceNum;
    private trackAnalytics;
    constructor(store: IStore, eventBus: EventBus, sampleWindowSize?: number, pushInterval?: number);
    start(): void;
    stop: () => void;
    private startLoop;
    private toAnalytics;
    private sendEvent;
    private handleStatsUpdate;
    private getTrackIdentifier;
}
