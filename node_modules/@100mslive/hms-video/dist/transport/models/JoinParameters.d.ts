export declare class JoinParameters {
    authToken: string;
    peerId: string;
    peerName: string;
    data: string;
    endpoint: string;
    autoSubscribeVideo: boolean;
    constructor(authToken: string, peerId: string, peerName?: string, data?: string, endpoint?: string, autoSubscribeVideo?: boolean);
}
