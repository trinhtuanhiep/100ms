import { InitConfig } from './models';
export default class InitService {
    private static handleError;
    static fetchInitConfig({ token, peerId, userAgent, initEndpoint, region, }: {
        token: string;
        peerId: string;
        userAgent: string;
        initEndpoint?: string;
        region?: string;
    }): Promise<InitConfig>;
}
export declare function getUrl(endpoint: string, peerId: string, userAgent: string, region?: string): string;
export declare function transformInitConfig(config: any): InitConfig;
