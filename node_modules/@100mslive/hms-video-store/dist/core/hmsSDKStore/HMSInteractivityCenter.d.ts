import { HMSPollCreateParams, HMSPollQuestionCreateParams, HMSPollQuestionResponseCreateParams, HMSSdk } from '@100mslive/hms-video';
import { IHMSInteractivityCenter } from '../schema';
export declare class HMSInteractivityCenter implements IHMSInteractivityCenter {
    private sdk;
    constructor(sdk: HMSSdk);
    private get sdkInteractivityCenter();
    createPoll(poll: HMSPollCreateParams): Promise<void>;
    startPoll(poll: string | HMSPollCreateParams): Promise<void>;
    stopPoll(poll: string): Promise<void>;
    addQuestionsToPoll(poll: string, questions: HMSPollQuestionCreateParams[]): Promise<void>;
    addResponsesToPoll(pollID: string, responses: HMSPollQuestionResponseCreateParams[]): Promise<void>;
}
